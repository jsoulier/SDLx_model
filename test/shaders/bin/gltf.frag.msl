#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_UniformLight
{
    float4 Light;
};

struct main0_out
{
    float4 out_var_SV_Target [[color(0)]];
};

struct main0_in
{
    float2 in_var_TEXCOORD0 [[user(locn0)]];
    float3 in_var_TEXCOORD1 [[user(locn1)]];
};

fragment main0_out main0(main0_in in [[stage_in]], constant type_UniformLight& UniformLight [[buffer(0)]], texture2d<float> ColorTexture [[texture(0)]], texture2d<float> NormalTexture [[texture(1)]], sampler ColorSampler [[sampler(0)]], sampler NormalSampler [[sampler(1)]])
{
    main0_out out = {};
    out.out_var_SV_Target = ColorTexture.sample(ColorSampler, in.in_var_TEXCOORD0) * (fast::clamp(dot((NormalTexture.sample(NormalSampler, in.in_var_TEXCOORD0).xyz * in.in_var_TEXCOORD1) * float3(0.5), -UniformLight.Light.xyz), 0.0, 1.0) + UniformLight.Light.w);
    return out;
}

